# 依赖与构建产物
node_modules/
dist/
*.log

# NSIS 工具链（9MB / 343 个文件）：本地编译独立卸载 EXE 用的第三方二进制，
# 与 Mac 构建无关，不必入库。**只忽略不删除**，本地仍可随时重新编译卸载器。
tools/nsistools/

# 注意：assets/vosk-models/*.zip（43.9MB 中文语音模型）**必须提交**，
# 否则打包后的应用没有离线语音，且小于 GitHub 单文件 100MB 上限。

# 系统杂项
.DS_Store
Thumbs.db

# 编辑器
.vscode/
.idea/
